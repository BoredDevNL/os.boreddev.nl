---
import BaseLayout from "../layouts/BaseLayout.astro";
import Hero from "../components/Hero.astro";
import SectionHeading from "../components/SectionHeading.astro";
import FeatureGrid from "../components/FeatureGrid.astro";
import ScreenshotGallery from "../components/ScreenshotGallery.astro";
import CallToAction from "../components/CallToAction.astro";

const features = [
  {
    title: "Nova Compositor",
    description: "Custom Wayland-style compositor."
  },
  {
    title: "LwIP Network stack",
    description: "lwIP stack with DHCP, DNS, and Berkeley sockets."
  },
  {
    title: "Native toolchain",
    description: "Build apps natively inside BoredOS with the Tiny C Compiler (TCC), Lua, and a rich C SDK."
  },
  {
    title: "Virtual Terminals",
    description: "10 independent TTYs (/dev/tty1–/dev/tty10), each with its own graphics buffer."
  }
];

const screenshots = [
  { src: "/branding/screenshot.jpg", alt: "BoredOS desktop screenshot" },
  { src: "/branding/screenshot2.jpg", alt: "BoredOS desktop overview" },
  { src: "/branding/screenshot3.jpg", alt: "BoredOS UI details" }
];

const baseUrl = import.meta.env.BASE_URL || "/";
const withBase = (path) => `${baseUrl}${path.replace(/^\//, "")}`;
---
<BaseLayout>
  <Hero
    title="An operating system, built from scratch."
    description="BoredOS is a from-scratch x86_64 operating system — FOSS, GPLv3-licensed, and thoroughly documented."
    image="/branding/wallpapers/boredos.png"
    imageAlt="BoredOS wallpaper"
    primaryAction={{ label: "Download", href: withBase("downloads") }}
    secondaryAction={{ label: "Read the docs", href: withBase("docs") }}
  />

  <section class="section">
    <div class="container glass" style="padding: clamp(32px, 5vw, 64px); border-radius: var(--radius-lg);">
      <SectionHeading
        eyebrow="Why BoredOS"
        title="More than a toy OS."
        description="A multi-core SMP kernel, a native C compiler, interactive math tools, and a window manager — all written from the ground up."
      />
      <FeatureGrid features={features} layout="balanced-4" />
      <div class="section-cta">
        <a class="btn btn-primary" href={withBase("features")}>See all features</a>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <SectionHeading
        eyebrow="Screens"
        title="A desktop you can actually use."
        description="BoredWM provides a familiar stacking interface with grid-snapping icons and a fuzzy-search launcher (Lumos)." />
      <ScreenshotGallery items={screenshots} layout="carousel" />
    </div>
  </section>

  <CallToAction
    title="An OS you can take apart."
    description="Write native C apps with the SDK, explore kernel internals, and join the community building BoredOS."
    primaryAction={{ label: "Explore docs", href: withBase("docs") }}
    secondaryAction={{ label: "Join Discord", href: "https://discord.gg/J2BxWaFAgY" }}
    tertiaryAction={{ label: "GitHub", href: "https://github.com/BoredOS/BoredOS" }}
  />
</BaseLayout>
